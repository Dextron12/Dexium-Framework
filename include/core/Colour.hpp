//
// Created by Dextron12 on 12/10/2025.
//

#ifndef DEXIUM_COLOUR_HPP
#define DEXIUM_COLOUR_HPP

#include <array>
#include <fmt/color.h>

/*

namespace Dexium {
    class Colour {
    public:
        Colour(uint8_t r8, uint8_t g8, uint8_t b8, uint8_t a8 = 255)
            : r(r8 / 255.0f), g(g8 / 255.0f), b(b8 / 255.0f), a(a8 / 255.0f) {}

        Colour(const std::array<uint8_t, 3>& colour) {

        }

        Colour(const std::array<uint8_t, 3>& rgb);
        Colour(const std::array<uint8_t, 4>& rgba);
        Colour(const std::array<float, 3>& rgb);
        Colour(const std::array<float, 4>& rgba);

        Colour(const fmt::color& col);

        bool operator==(const Colour& other) const {
            return r == other.r && g == other.g && b == other.b && a == other.a;
        }

    private:
        float r, g, b, a;

    };
}

*/

#endif //DEXIUM_COLOUR_HPP