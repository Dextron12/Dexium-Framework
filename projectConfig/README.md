# Dexium-Framework Project configurations

This sub-folder of ** Dexium-Framework ** defines CMake targets per platform

Currently, supports Linux(Natively developed on) and Windows (Occasionally tested on Win11, but mostly done for major changes)

It may also contain CMakeList files for building dependencies from thrid_party to properly build and integrate them as moduels into the dexium library
